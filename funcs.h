#pragma once
#include <vector>

std::vector<int> makeVector(int);
std::vector<int> goodVibes(const std::vector<int>&);
void gogeta(std::vector<int>&, std::vector<int>&);
std::vector<int> sumPairWise(const std::vector<int>&, const std::vector<int>&);
